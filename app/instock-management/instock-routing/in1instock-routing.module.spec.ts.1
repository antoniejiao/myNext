import { InstockRoutingModule } from './instock-routing.module';

describe('InstockRoutingModule', () => {
  let instockRoutingModule: InstockRoutingModule;

  beforeEach(() => {
    instockRoutingModule = new InstockRoutingModule();
  });

  it('should create an instance', () => {
    expect(instockRoutingModule).toBeTruthy();
  });
});
